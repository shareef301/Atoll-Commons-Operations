import {sqliteTable,text,integer,uniqueIndex,index} from 'drizzle-orm/sqlite-core';
export const workspaces=sqliteTable('workspaces',{id:text('id').primaryKey(),ownerEmail:text('owner_email').notNull(),data:text('data').notNull(),revision:integer('revision').notNull().default(0),createdAt:text('created_at').notNull()});
export const memberships=sqliteTable('memberships',{id:text('id').primaryKey(),workspaceId:text('workspace_id').notNull().references(()=>workspaces.id),email:text('email').notNull(),name:text('name').notNull(),role:text('role').notNull(),projects:text('projects').notNull().default('[]'),governance:integer('governance').notNull().default(0),authorityRef:text('authority_ref').notNull().default('')},t=>[uniqueIndex('memberships_email_unique').on(t.email),index('memberships_workspace_idx').on(t.workspaceId)]);
export const identities=sqliteTable('auth_identities',{id:text('id').primaryKey(),issuer:text('issuer').notNull(),subject:text('subject').notNull(),email:text('email').notNull(),name:text('name').notNull(),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('auth_identity_email').on(t.email),uniqueIndex('auth_identity_subject').on(t.issuer,t.subject)]);
export const sessions=sqliteTable('auth_sessions',{tokenHash:text('token_hash').primaryKey(),identityId:text('identity_id').notNull().references(()=>identities.id,{onDelete:'cascade'}),expiresAt:integer('expires_at').notNull()},t=>[index('auth_sessions_expiry').on(t.expiresAt)]);
export const authFlows=sqliteTable('auth_flows',{tokenHash:text('token_hash').primaryKey(),state:text('state').notNull(),nonce:text('nonce').notNull(),verifier:text('verifier').notNull(),expiresAt:integer('expires_at').notNull()});
