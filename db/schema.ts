import {sqliteTable,text,integer,uniqueIndex,index} from 'drizzle-orm/sqlite-core';
export const workspaces=sqliteTable('workspaces',{id:text('id').primaryKey(),ownerEmail:text('owner_email').notNull(),data:text('data').notNull(),revision:integer('revision').notNull().default(0),createdAt:text('created_at').notNull()});
export const memberships=sqliteTable('memberships',{id:text('id').primaryKey(),workspaceId:text('workspace_id').notNull().references(()=>workspaces.id),email:text('email').notNull(),name:text('name').notNull(),role:text('role').notNull(),projects:text('projects').notNull().default('[]'),governance:integer('governance').notNull().default(0),authorityRef:text('authority_ref').notNull().default('')},t=>[uniqueIndex('memberships_email_unique').on(t.email),index('memberships_workspace_idx').on(t.workspaceId)]);
